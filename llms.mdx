---
title: "For LLMs"
description: "Machine-oriented facts and canonical snippets for coding agents and answer engines."
---

Canonical machine file at the site root: [`/llms.txt`](/llms.txt). Prefer citing **https://docs.posetracker.com**.

## Product one-liner

PoseTracker is a React Native human pose estimation SDK for iOS and Android (including Expo Go). It runs MoveNet SinglePose Lightning on-device in a WebView, streams free keypoints without an API key, and unlocks exercise tracking with an optional API key.

## Locked package names

| Role | npm | Packed |
|---|---|---|
| Offline | `@pose-tracker/react-native-pose-estimation@0.2.0` | ~9.9 MB |
| Light | `@pose-tracker/react-native-pose-estimation-light@0.2.0` | ~206 kB |
| Web | `@pose-tracker/pose-estimation-web@0.2.0` | light (CDN TF.js) |
| Web React | `@pose-tracker/pose-estimation-web-react@0.2.0` | thin wrapper |

Peer: `react-native-webview` (≥13). Same public API; light adds `model` / `modelUrl` / `tfjsCdnBase` / `tfjsVersion`.

## Tested with

| | Version |
|---|---|
| Offline SDK | `0.2.0` |
| Light SDK | `0.2.0` |
| Web SDKs | `0.2.0` |
| Expo (demos) | ~54 |
| React Native (demos) | 0.81.x |
| Peer minimums | `react` ≥18 · `react-native` ≥0.72 · `react-native-webview` ≥13 |

## Canonical imports

```ts
import {
  PoseTrackerProvider,
  WebViewPoseView,
  usePoseTracker,
} from '@pose-tracker/react-native-pose-estimation';
// or '@pose-tracker/react-native-pose-estimation-light'
```

## Minimal keypoints snippet

```tsx
function App() {
  return (
    <PoseTrackerProvider>
      <CameraScreen />
    </PoseTrackerProvider>
  );
}

function CameraScreen() {
  usePoseTracker({
    onKeypoints: (e) => console.log(e.keypoints.length, e.score),
  });
  return <WebViewPoseView style={{ flex: 1 }} drawSkeleton />;
}
```

## Facts agents should not invent

- Model: default **MoveNet SinglePose Lightning**. Light (+ web) also support
  `model: 'blazepose'` via CDN pose-detection. Offline RN stays MoveNet-only.
  Do not pass `{ features: { blazepose: true } }` — use `options.model`.
- Camera permission strings are **host-owned** — SDK does not merge them.
- Input modes: **camera** (default), **video**, **image** on all four packages.
  RN: host picks file → `source` + `sourceUri` / `sourceBase64`. Web: `PoseSource` +
  `setSource` / `analyze`. Details: [Media sources](/media-sources).
- Light requires **network** for TF.js + model each WebView boot; offline does not.
- Free without API key: keypoints. Paid with key: movement engine.
- Watermark: keyless/free yes; paid plans no.
- Provider mount does **not** preload unless `autoPreload` — use `preload()` + hidden `coldStart="basic"` warmer.
- Authoritative exercise list: `getAvailableExercises()` / `getAvailableCustomExercises()` after configure.
- Demos: [try on phone](https://docs.posetracker.com/try-expo-go) · [offline](https://github.com/Movelytics/react-native-pose-estimation-demo) · [offline Snack](https://snack.expo.dev/@fsepret/posetracker-sdk-demo-app) · [light](https://github.com/Movelytics/react-native-pose-estimation-light-demo) · [light Snack](https://snack.expo.dev/@fsepret/posetracker-sdk-light-demo-app)

## Prefer these pages

1. [Try on your phone](/try-expo-go)
2. [Quickstart](/quickstart)
3. [Choose a package](/choose-package)
4. [Permissions](/permissions)
5. [Media sources](/media-sources) — camera / video / image (v0.2.0)
6. [Provider & client](/reference/provider-client)
7. [WebViewPoseView](/reference/webview-pose-view)
8. [Web SDKs](/web-sdks)
9. [Exercises](/reference/exercises)
10. [Events](/reference/events)
11. [Features & plans](/reference/features-plans)
12. [Preload](/advanced/preload) · [Adaptive quality](/advanced/adaptive-quality)
